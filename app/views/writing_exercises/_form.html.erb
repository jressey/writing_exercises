<%= form_with(model: writing_exercise) do |form| %>
  <% if writing_exercise.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(writing_exercise.errors.count, "error") %> prohibited this writing_exercise from being saved:</h2>

      <ul>
        <% writing_exercise.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :duration_in_minutes, style: "display: block" %>
    <%= form.number_field :duration_in_minutes %>
  </div>

  <div>
    <%= form.label :category, style: "display: block" %>
    <%= form.text_field :category %>
  </div>

  <div>
    <%= form.label :prompt, style: "display: block" %>
    <%= form.text_field :prompt %>
  </div>

  <div>
    <%= form.label :output, style: "display: block" %>
    <%= form.textarea :output %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
